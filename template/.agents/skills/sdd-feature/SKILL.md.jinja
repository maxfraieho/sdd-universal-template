---
name: sdd-feature
description: Guide and execute the SDD Feature Development workflow (Template 1) for new features, endpoints, or UI components in {{ project_name }}.
---

# SDD Feature Skill

When activated for a new feature task:

1. Read `docs/for-agents/sdd-development-methodology.md`, section "ШАБЛОН 1" (Feature Development), and follow it literally — do not invent your own sequence.
2. Before writing the spec, read `.specify/constitution.md` — every invariant defined there must be reflected in `spec.md`.
3. Determine the next free `specs/<NNN>-<slug>/` by running `ls specs/` — never guess the number from memory.
{%- if code_intelligence != "none" %}
4. Run impact analysis via {{ code_intelligence }} before modifying any existing symbol.
{%- endif %}
5. Follow the MUST rules in `AGENTS.md`.
6. Final report must state: files created{% if code_intelligence != "none" %}, {{ code_intelligence }} impact-analysis output{% endif %}{% if test_command %}, `{{ test_command }}` output{% endif %}, and whether `bash bin/sdd_verify.sh` passed.
